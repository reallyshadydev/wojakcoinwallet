# Build Android debug APK and upload as workflow artifact + optional release asset.
#
# Triggers: push to main, or manual (workflow_dispatch).
#
# Uses Node 24 for JavaScript-based actions (see GitHub deprecation of Node 20 on runners).

name: Build Android

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

env:
  # Opt in to Node.js 24 for @actions/* runtimes (Node 20 deprecated on GHA)
  # https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-android:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write # gh release upload + artifact
      actions: write # upload-artifact

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps || npm install --legacy-peer-deps

      - name: Build web (Next.js static export)
        run: npm run build

      - name: Sync Capacitor Android
        run: npx cap sync android

      - name: Build debug APK
        working-directory: android
        run: chmod +x gradlew && ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v6
        with:
          name: wojakcoinwallet-android-debug-${{ github.run_number }}-${{ github.sha }}
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Add APK to release v1.2.0
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if ! gh release view v1.2.0 &>/dev/null; then
            echo "Release v1.2.0 not found; skipping upload."
            exit 0
          fi
          gh release upload v1.2.0 android/app/build/outputs/apk/debug/app-debug.apk --clobber
          echo "Uploaded Android APK to release v1.2.0"

      - name: Summary
        run: |
          echo "### Android build (artifact + release upload)" >> "$GITHUB_STEP_SUMMARY"
          echo "Download **app-debug.apk** from this job's **Artifacts** section." >> "$GITHUB_STEP_SUMMARY"
          echo "Release target: \`v1.2.0\` (if present)." >> "$GITHUB_STEP_SUMMARY"
          echo "SHA: \`${{ github.sha }}\`" >> "$GITHUB_STEP_SUMMARY"
